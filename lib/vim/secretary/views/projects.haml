.box
  .box-header
    %span All Projects
  .box-inner
    %ul
      - @projects.each do |project|
        %li
          %a{href: "/projects/#{project.id}"}
            = project.name
            (#{project.punches.size})
